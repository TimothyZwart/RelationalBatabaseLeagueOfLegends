SELECT C.char_name
FROM Characters C
WHERE  NOT EXISTS(
SELECT *
FROM ABILITY AB
WHERE EXISTS(
SELECT *
FROM ABILITY_UTILITY AU
WHERE AU.ABIL_NUM = AB.ABIL_NUM
AND AB.CHAR_ID=C.CHAR_ID
AND NOT AU.ABIL_DMG > 100));